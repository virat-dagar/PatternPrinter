services:
  - type: web
    name: pattern-printer
    runtime: python
    plan: free
    buildCommand: python3 -m py_compile backend/server.py backend/patterns.py backend/cli.py
    startCommand: python3 -m backend.server
    healthCheckPath: /health
